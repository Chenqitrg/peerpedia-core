EXAMPLES
  # Follow by user ID
  peerpedia follow abc12345

  # Follow by @name
  peerpedia follow @alice

Following a user adds their articles to your feed (article list --feed).
The followed user receives a notification.

SEE ALSO
  unfollow           Stop following
  article list --feed    See articles from followed users
  alias set          Give a followed user a nickname